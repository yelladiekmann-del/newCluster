rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Users can only read/write their own session files
    match /sessions/{sessionId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == sessionId;
    }
  }
}
